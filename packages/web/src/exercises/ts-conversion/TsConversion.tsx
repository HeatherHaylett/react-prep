import { ExerciseShell } from "../../components/ExerciseShell";

// GOAL: Convert JavaScript components to TypeScript
// ACCEPTANCE CRITERIA:
// - Start with provided JS components (as comments or separate files)
// - Add proper TypeScript types
// - Use interfaces for props
// - Type event handlers correctly
// - Type useState with generics
// - No use of `any`

export default function TsConversion() {
  return (
    <ExerciseShell
      title="TypeScript Conversion"
      description="Convert JavaScript components to TypeScript."
    >
      <p>TODO: Implement TypeScript conversion exercise</p>
    </ExerciseShell>
  );
}
